{
  "name": "formulario-atc20",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "ATC-20 field evaluation form (independent service)",
  "scripts": {
    "test:unit": "node --test",
    "test:e2e": "playwright test",
    "test": "npm run test:unit && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
