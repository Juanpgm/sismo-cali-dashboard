# Raw survey Excel — contains PII (email, telefono, nombre_contacto, etc.)
# Kept local only; never publish.
*.xlsx
# Exception: the assignments roster is a published dashboard artifact (no PII),
# downloaded by the "Descargar tabla (Excel)" button.
!web/data/asignaciones.xlsx

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# Vercel
.vercel/

# Secrets / credentials
.env
.env.*
*service-account*.json
*credentials*.json

# Tooling artifacts
.atl/

# Knowledge-graph sources (large PDFs/txt, re-downloadable via
# knowledge/build_kg.py --extract)
knowledge/fuentes/

.vercel
.env*
