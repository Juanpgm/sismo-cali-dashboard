# Raw survey Excel — contains PII (email, telefono, nombre_contacto, etc.)
# Kept local only; never publish.
*.xlsx
# Exception: the assignments roster is a published dashboard artifact (no PII),
# downloaded by the "Descargar tabla (Excel)" button.
!web/data/asignaciones.xlsx
# Exception: the full published dataset (already PII-stripped by refresh_data.py),
# downloaded by the "Descargar datos (xlsx)" button.
!web/data/inspections.xlsx

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# Vercel
.vercel/

# Secrets / credentials
.env
.env.*
*service-account*.json
*credentials*.json

# Tooling artifacts
.atl/

# Knowledge-graph sources (large PDFs/txt, re-downloadable via
# knowledge/build_kg.py --extract)
knowledge/fuentes/

.vercel
.env*

# subproyecto con repo git propio (normalizador_data_sismo_cali)
integracion_F1/
# artefacto local del cruce (la fuente real es Firestore)
web/data/cruce_criticos_survey.json
credenciales_inspectores.csv
Personas en campo con stickers.xlsx

# Bundles locales (p.ej. transferencia de secretos) — nunca commitear
*.zip
