"""Modules absorbed from `integracion_F1`, copy-with-provenance per job slice
(design.md ADR-2). See `PROVENANCE.md` in this package for the source/date
table; every copied file carries a `# Ported from ...` header.
"""
