{
  "//": "Build config for the dashboard-refresh cron service. Deploy from this dir: `railway up --service dashboard-refresh`. cronSchedule lives on the service instance (set via backend/scripts/railway_services.py), never here.",
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "restartPolicyType": "NEVER",
    "numReplicas": 1
  }
}
