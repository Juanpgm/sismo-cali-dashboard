{
  "//": "Build config for the dashboard-refresh cron service. Deploy from this dir: `railway up --service dashboard-refresh`. cronSchedule lives on the service instance (set via integracion_F1/scripts/railway_setup.py), never here.",
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "restartPolicyType": "NEVER",
    "numReplicas": 1
  }
}
